import { StyledHeader } from './Header.styles';
import Navbar from './Navbar/Navbar';

const Header = () => {
  return (
    <StyledHeader>
      <Navbar />
    </StyledHeader>
  );
};

export default Header;
